/**
 * Hooks de JuegaHipHop
 *
 * El hook useGameSession fue integrado directamente en GameContainer.tsx
 * para mantener toda la lógica de sesión en un solo lugar.
 *
 * Los hooks futuros se agregarán aquí:
 * - useGameCatalog   → catálogo dinámico
 * - useSyncQueue     → cola de sincronización local-first
 * - useCampaignView  → visualización de campañas
 */
export {}
